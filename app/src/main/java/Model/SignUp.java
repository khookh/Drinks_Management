package Model;

public class SignUp {
    private static String signedup = "error";
    public static String SignUp(String nickname, String email, String password, Integer age, String sex){
        //check si l'user n'existe pas déjà
        //crée le user -> signedup = "Signed Up"
        return signedup;
    }
}
